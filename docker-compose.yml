services:
  yomi:
    build: ./web
    container_name: yomi
    restart: unless-stopped
    environment:
      MANGAWORLD_BASE: ${MANGAWORLD_BASE:-}
    volumes:
      - yomi-data:/data
    networks:
      - nginx-network

volumes:
  yomi-data:

networks:
  nginx-network:
    external: true
